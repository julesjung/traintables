[package]
name = "siri"
version = "0.1.0"
edition = "2024"

[dependencies]
traintables_core = { path = "../core" }
tokio = { version = "1.48.0", features = ["full"] }
reqwest = { version = "0.12.26", default-features = false, features = ["rustls-tls", "json"] }
